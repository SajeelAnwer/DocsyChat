# These are the contents of .env file.
# create a .env file in the backend folder.
# copy and paste this in the .env file and add the api keys.

# =============================================
# DocsyChat v3 — Environment Configuration
# =============================================

# ── AI Provider ──────────────────────────────
AI_PROVIDER=gemini
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# ── Supabase (Database) ───────────────────────
# Project URL from: Supabase Dashboard → Settings → API
SUPABASE_URL=Your_supabase_url_here(https://xxxxxxx.supabase.co)
# Anon/Public key from: Supabase Dashboard → Settings → API
SUPABASE_ANON_KEY=your_Key_here (long string)
# Password set when creating the Supabase project
SUPABASE_DB_PASSWORD=your_Db_pass_here (project pass not login pass)

# ── JWT Auth ──────────────────────────────────
JWT_SECRET=docsychat_jwt_secret_change_in_production_v3
JWT_EXPIRES_IN=7d

# ── Email (Gmail SMTP) ────────────────────────
# Gmail address that sends verification emails
EMAIL_FROM=email which sends the verification email
# Gmail App Password (NOT your regular password)
# Get it: Google Account → Security → 2-Step Verification ON → App Passwords → DocsyChat
EMAIL_APP_PASSWORD=your_pass_here

# ── Server ────────────────────────────────────
PORT=5000
MAX_FILE_SIZE_MB=10

# ── RAG Settings ─────────────────────────────
RAG_TOP_K=5
RAG_CHUNK_SIZE=1500
RAG_CHUNK_OVERLAP=200
